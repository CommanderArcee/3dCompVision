# Execution Plan (Full Pipeline)

This file tracks the actionable implementation order for the full project based on `plan.md`.

## Scope

- Implement all core steps from Step 0 through Step 11.
- Keep optional branches implemented behind flags:
  - Dense optical flow visualization
  - Alternate PCA-descriptor classifier branch
  - Segmentation IoU if ground truth is available

## Build Order

1. Project setup, dependencies, and folder structure
2. Dataset tooling for real datasets (Objectron + COIL-100)
3. Frame extraction and preprocessing
4. Segmentation and mask generation
5. SIFT extraction and feature matching
6. Two-view initialization and triangulation
7. Incremental reconstruction with PnP + triangulation
8. Optical flow module
9. PCA alignment of point cloud
10. SVM classification with confusion matrix
11. Evaluation metrics and report artifacts
12. Master integration script with CLI flags

## Datasets

- Primary geometric verification: Objectron video clips
- Classification and multi-view verification: COIL-100

## Deliverables Generated by Code

- Intermediate images and overlays
- Match visualizations
- Sparse point cloud (`.ply`)
- PCA-aligned point cloud (`.ply`)
- Optical flow maps
- Classification metrics and confusion matrix image
- Reconstruction metrics JSON
